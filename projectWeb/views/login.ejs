<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
    <div class="container p-5">
        <h1 class="text-center mb-5 text-uppercase text-muted">NodeJS Login</h1>
        <div class="row">
            <div class="col-sm-5 bg-light rounded py-3">
                <h2 class="text-center">Login</h2>
                <!-- แสดงข้อความ error ถ้ามี -->
                <% if (typeof login_errors !== 'undefined' && login_errors.length > 0) { %>
                    <div class="alert alert-danger">
                        <ul>
                            <% login_errors.forEach(error => { %>
                                <li><%= error %></li>
                            <% }) %>
                        </ul>
                    </div>
                <% } %>
                <form action="/login" method="post" novalidate>
                    <div class="form-group mt-2">
                        <label for="_email">Email</label>
                        <input type="text" name="user_email" class="form-control" placeholder="Email" required>
                        <!-- แสดงข้อความบังคับให้กรอก Email -->
                        <span class="text-danger">
                            <% if (typeof login_errors !== 'undefined' && login_errors.some(error => error.includes('Email'))) { %>
                                Email is required.
                            <% } %>
                        </span>
                    </div>
                    <div class="form-group mt-2">
                        <label for="_pass">Password</label>
                        <input type="password" name="user_pass" id="_pass" class="form-control" placeholder="Password" required>
                        <!-- แสดงข้อความบังคับให้กรอก Password -->
                        <span class="text-danger">
                            <% if (typeof login_errors !== 'undefined' && login_errors.some(error => error.includes('Password'))) { %>
                                Password is required.
                            <% } %>
                        </span>
                    </div>
                    <button type="submit" class="mt-2 btn btn-primary">Login</button>
                </form>
            </div>
            <div class="col-sm-6"></div>
        </div>
    </div>
</body>
</html>
